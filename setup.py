from setuptools import find_packages, setup

setup(
    name = "ctc_utils",
    version = "0.0.1",
    description="General purpose functions for a cell type classification project",
    packages = find_packages(),
    zip_safe=False,
)